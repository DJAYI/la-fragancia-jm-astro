---
interface Props {
  href: string;
  label: string;
}

const { href, label } = Astro.props;
---

<a
  href={href}
  aria-label={label}
  class="p-3 rounded-full aspect-square border-2 border-primary group hover:bg-primary transition"
>
  <slot />
</a>
